Name:           ros-kinetic-moveit-sim-controller
Version:        0.1.0
Release:        0%{?dist}
Summary:        ROS moveit_sim_controller package

Group:          Development/Libraries
License:        BSD
URL:            https://github.com/davetcoleman/moveit_sim_controller
Source0:        %{name}-%{version}.tar.gz

Requires:       ros-kinetic-moveit-core
Requires:       ros-kinetic-moveit-ros-planning
Requires:       ros-kinetic-ros-control-boilerplate
Requires:       ros-kinetic-roscpp
Requires:       ros-kinetic-rosparam-shortcuts
BuildRequires:  ros-kinetic-catkin
BuildRequires:  ros-kinetic-moveit-core
BuildRequires:  ros-kinetic-moveit-ros-planning
BuildRequires:  ros-kinetic-ros-control-boilerplate
BuildRequires:  ros-kinetic-roscpp
BuildRequires:  ros-kinetic-roslint
BuildRequires:  ros-kinetic-rosparam-shortcuts

%description
A simulation interface for a hardware interface for ros_control, and loads
default joint values from SRDF

%prep
%setup -q

%build
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/kinetic/setup.sh" ]; then . "/opt/ros/kinetic/setup.sh"; fi
mkdir -p obj-%{_target_platform} && cd obj-%{_target_platform}
%cmake .. \
        -UINCLUDE_INSTALL_DIR \
        -ULIB_INSTALL_DIR \
        -USYSCONF_INSTALL_DIR \
        -USHARE_INSTALL_PREFIX \
        -ULIB_SUFFIX \
        -DCMAKE_INSTALL_LIBDIR="lib" \
        -DCMAKE_INSTALL_PREFIX="/opt/ros/kinetic" \
        -DCMAKE_PREFIX_PATH="/opt/ros/kinetic" \
        -DSETUPTOOLS_DEB_LAYOUT=OFF \
        -DCATKIN_BUILD_BINARY_PACKAGE="1" \

make %{?_smp_mflags}

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/kinetic/setup.sh" ]; then . "/opt/ros/kinetic/setup.sh"; fi
cd obj-%{_target_platform}
make %{?_smp_mflags} install DESTDIR=%{buildroot}

%files
/opt/ros/kinetic

%changelog
* Thu Oct 20 2016 Dave Coleman <davetcoleman@gmail.com> - 0.1.0-0
- Autogenerated by Bloom

