Name:           ros-lunar-moveit-sim-controller
Version:        0.1.0
Release:        0%{?dist}
Summary:        ROS moveit_sim_controller package

Group:          Development/Libraries
License:        BSD
URL:            https://github.com/davetcoleman/moveit_sim_controller
Source0:        %{name}-%{version}.tar.gz

Requires:       ros-lunar-moveit-core
Requires:       ros-lunar-moveit-ros-planning
Requires:       ros-lunar-ros-control-boilerplate
Requires:       ros-lunar-roscpp
Requires:       ros-lunar-rosparam-shortcuts
BuildRequires:  ros-lunar-catkin
BuildRequires:  ros-lunar-moveit-core
BuildRequires:  ros-lunar-moveit-ros-planning
BuildRequires:  ros-lunar-ros-control-boilerplate
BuildRequires:  ros-lunar-roscpp
BuildRequires:  ros-lunar-roslint
BuildRequires:  ros-lunar-rosparam-shortcuts

%description
A simulation interface for a hardware interface for ros_control, and loads
default joint values from SRDF

%prep
%setup -q

%build
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/lunar/setup.sh" ]; then . "/opt/ros/lunar/setup.sh"; fi
mkdir -p obj-%{_target_platform} && cd obj-%{_target_platform}
%cmake .. \
        -UINCLUDE_INSTALL_DIR \
        -ULIB_INSTALL_DIR \
        -USYSCONF_INSTALL_DIR \
        -USHARE_INSTALL_PREFIX \
        -ULIB_SUFFIX \
        -DCMAKE_INSTALL_LIBDIR="lib" \
        -DCMAKE_INSTALL_PREFIX="/opt/ros/lunar" \
        -DCMAKE_PREFIX_PATH="/opt/ros/lunar" \
        -DSETUPTOOLS_DEB_LAYOUT=OFF \
        -DCATKIN_BUILD_BINARY_PACKAGE="1" \

make %{?_smp_mflags}

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/lunar/setup.sh" ]; then . "/opt/ros/lunar/setup.sh"; fi
cd obj-%{_target_platform}
make %{?_smp_mflags} install DESTDIR=%{buildroot}

%files
/opt/ros/lunar

%changelog
* Tue Jun 20 2017 Dave Coleman <davetcoleman@gmail.com> - 0.1.0-0
- Autogenerated by Bloom

